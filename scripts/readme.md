# Supporting Scripts for Alias

The scripts are focused on scraping the name data.  These are only needed for updating the data.
The required packages can be installed with:

    pip install -r requirements.txt
